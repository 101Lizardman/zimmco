# Voice lines

Drop an MP3 in this folder named after a beverage id and it will play automatically ~260ms after the coin-drop sound on vend, with no code changes.

| File | Beverage |
| --- | --- |
| `klassic.mp3` | Klassic |
| `atomka.mp3` | Atomka! |
| `hammer.mp3` | Hammer |
| `frost.mp3` | Frost |
| `geiger.mp3` | Geiger |
| `bomba.mp3` | Bomba |
| `aurora.mp3` | Aurora |
| `iskra.mp3` | Iskra |

Nothing here yet — that's fine. A missing file fails silently (see `playVoiceLine` in `index.html`), so the machine works with just the synthesised coin-drop sound until you add recordings.
